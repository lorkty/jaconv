#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

struct Mesearments_equal{
  double worknum;
  double nominalnum;
}mmlength, cmlength, mlength, kmlength;

float equalmeters[4] = {0.001, 0.01, 1, 1000}; // mm, cm, m, km - 0.001, 0.01, 1, 1000

